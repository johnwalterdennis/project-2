/* 
Name: **********
***ID:  *********
Section:  *********
*/

import java.util.Random;
import java.util.Scanner;

class Main {
  public static void main(String[] args) {
    //These first two lines pick a random number
    //between 0 and 99 (inclusive).
   Random myRand=new Random();
    int answer=myRand.nextInt(100);
    
    Scanner myScan1=new Scanner(System.in);
      System.out.println("A number is chosen between 1 to 100 Guess the number withing 7 trials pick an number");
    int usernumber1 = myScan1.nextInt();
    if (usernumber1 > answer) {
  System.out.println("too high"+"\n");
    }
    else if (usernumber1 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("too low" + "\n");
    }
    
Scanner myScan2=new Scanner(System.in);
  System.out.println("Enter your second guess");
  int usernumber2 = myScan2.nextInt();
  if (usernumber2 > answer) {
  System.out.println("too high"+"\n");
    }
    else if (usernumber2 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("too low" + "\n");
    }
Scanner myScan3=new Scanner(System.in);
  System.out.println("Enter your third guess");
  int usernumber3 = myScan3.nextInt();
  if (usernumber3 > answer) {
  System.out.println("too high"+"\n");
    }
    else if (usernumber3 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("too low" + "\n");
    }
Scanner myScan4=new Scanner(System.in);
  System.out.println("Enter your fourth guess");
  int usernumber4 = myScan4.nextInt();
  if (usernumber4 > answer) {
  System.out.println("too high"+"\n");
    }
    else if (usernumber4 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("too low" + "\n");
    }
Scanner myScan5=new Scanner(System.in);
  System.out.println("Enter your fifth guess");
  int usernumber5 = myScan5.nextInt();
  if (usernumber5 > answer) {
  System.out.println("too high"+"\n");
    }
    else if (usernumber5 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("too low" + "\n");
    }
Scanner myScan6=new Scanner(System.in);
  System.out.println("Enter your sixth guess");
  int usernumber6 = myScan2.nextInt();
  if (usernumber6 > answer) {
  System.out.println("too high"+"\n");
    }
    else if (usernumber6 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("too low" + "\n");
    }
Scanner myScan7=new Scanner(System.in);
  System.out.println("Enter your seventh guess");
  int usernumber7 = myScan2.nextInt();
  if (usernumber7 > answer) {
  System.out.println("the correct number was "+ answer);
    }
    else if (usernumber2 == answer) {
  System.out.println("correct"+ "\n");
      return;
    }
    else {
  System.out.println("the correct number was " + answer);
    }
  }
} 
    //The number the user is trying to guess is
    //stored in the variable called answer:
    

    
    
    //Create a Scanner object.  You'll need it
    //to read in the users guesses.

    //Ask the user to enter a guess
    //Read in the users first guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.

    //Ask the user to enter the second guess
    //Read in the users second guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.

    //Ask the user to enter the third guess
    //Read in the users third guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.


    //Ask the user to enter the forth guess
    //Read in the users forth guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.


    //Ask the user to enter the fifth guess
    //Read in the users fifth guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.


    //Ask the user to enter the sixth guess
    //Read in the users sixth guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.


    //Ask the user to enter the seventh guess
    //Read in the users seventh guess
    //Check if that guess is correct
    //  (the number entered matches answer)
    //  If they guessed correctly, congratulate them and return
    //  If they were too high or low, tell them.

    //Tell them the correct answer.
  
